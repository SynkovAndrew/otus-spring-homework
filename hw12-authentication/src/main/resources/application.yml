spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
    driverClassName: org.h2.Driver
    initialization-mode: always
  jpa:
    generate-ddl: false
    show-sql: false
    hibernate:
      ddl-auto: none