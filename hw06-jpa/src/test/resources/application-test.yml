spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
    driverClassName: org.h2.Driver
    initialization-mode: always
    data: data-test.sql
    schema: schema-test.sql
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none